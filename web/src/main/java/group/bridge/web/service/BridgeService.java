package group.bridge.web.service;

import group.bridge.web.entity.Bridge;

import java.util.List;

public interface BridgeService extends BaseService<Bridge, Integer>{
//    Bridge addBridge(Bridge bridge);
//    void deleteBridge(Integer bID);
//    List<Bridge> getAllBridge();
//    Bridge updateBridge(Bridge bridge);
//    Bridge getBridgeByID(Integer bID);
}
